apiVersion: v1
kind: ConfigMap
metadata:
  name: ext-auth-tokens
  namespace: backend
data:
  # Format: "token1:user_id1,token2:user_id2"
  # Add or remove tokens here; restart the pod to pick up changes.
  tokens: "my-secret-token-001:user_001,my-secret-token-002:user_002"
